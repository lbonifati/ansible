<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Cotización del Dólar</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #1e1e1e;
      color: #f0f0f0;
      margin: 0;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #ffffff;
      margin-bottom: 40px;
    }

    .cotizaciones {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
      gap: 20px;
    }

    .bloque {
      background-color: #2c2c2c;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 4px 8px rgba(0,0,0,0.5);
      transition: transform 0.2s ease;
    }

    .bloque:hover {
      transform: scale(1.02);
    }

    .bloque h2 {
      margin-top: 0;
      font-size: 1.2em;
      color: #00bcd4;
    }

    .bloque p {
      margin: 8px 0;
      font-size: 0.95em;
      border-bottom: 1px solid #444;
      padding-bottom: 4px;
    }

    .bloque p:last-child {
      border-bottom: none;
    }
  </style>
</head>
<body>
  <h1>Cotización del Dólar</h1>
  <div class="cotizaciones">
    {% for cotizacion in consulta_cotizaciones.results %}
      <div class="bloque">
        <h2>{{ cotizacion.json.nombre }}</h2>
        <p><strong>Moneda:</strong> {{ cotizacion.json.moneda }}</p>
        <p><strong>Compra:</strong> {{ cotizacion.json.compra }}</p>
        <p><strong>Venta:</strong> {{ cotizacion.json.venta }}</p>
        <p><strong>Última actualización:</strong> {{ cotizacion.json.fechaActualizacion }}</p>
      </div>
    {% endfor %}
  </div>
</body>
</html>
